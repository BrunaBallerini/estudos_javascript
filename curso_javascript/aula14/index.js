// Constantes

const nome = 'Bruna';
console.log(nome);

console.log(typeof nome) // para saber o tipo da variavel
console.log(typeof(typeof nome)) // para saber o tipo da variavel

const primeiro = 2;
const segundo = 3;
const resultado = primeiro + segundo;
console.log(resultado)

// nome de variável não pode começar com números
// não pode conter traços - e espaços
// camelCase
// não pode modificar valor
// não pode ser inicializada